FROM postgrest/postgrest:v12.2.0

# PostgREST runs on port 3000 by default
EXPOSE 3000

# Configuration is provided via environment variables:
# - PGRST_DB_URI
# - PGRST_DB_SCHEMAS
# - PGRST_DB_ANON_ROLE
# - PGRST_OPENAPI_SERVER_PROXY_URI
